#!/bin/sh
source bin/lilarc

RSYNC_OPTIONS=" \
--archive \
--force \
--delete \
--progress \
--compress \
--checksum \
--exclude-from=bin/prod/rsync_exclude";

rsync $RSYNC_OPTIONS ./ $REMOTE:$REMOTE_DIR
