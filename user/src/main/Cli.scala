package lila.user

import play.api.libs.concurrent.Execution.Implicits._

private[user] final class Cli(env: Env) {

  import env._

  // def rewriteHistory = historyRepo.fixAll inject "History rewritten"
}
