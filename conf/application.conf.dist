include "base"

# Define application domain name
net.domain = "l.org"

# Use local artificial intelligence 
ai {
  server = false
  client = false
  stockfish {
    debug = false
    hash_size = 128
    threads = 2
  }
}
