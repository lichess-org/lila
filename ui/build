#!/bin/sh
. bin/lilarc

target=${1-dev}

mkdir -p public/compiled

for app in editor puzzle round analyse; do
  cd ui/$app
  npm install ../chessli
  npm install ../game
  npm install && gulp $target
  cd -
done
