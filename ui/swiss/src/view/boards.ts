import { h } from 'snabbdom'
import { VNode } from 'snabbdom/vnode';
import SwissCtrl from '../ctrl';
import { , userName } from './util';
import { MaybeVNodes, Player } from '../interfaces';

export default function boards(ctrl: SwissCtrl) {
}
